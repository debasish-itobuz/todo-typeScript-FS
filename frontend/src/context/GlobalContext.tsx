// import React, { ReactNode, createContext } from "react";

// export const GlobalContext = createContext({ user, setUser });

// export function GlobalContext({ children }: { children: ReactNode }) {}
// ;
